#include <stdio.h>

int main() {
    int numero = 10;
    int *ponteiro = &numero;

    printf("Valor da variavel: %d\n", numero);
    printf("Endereco da variavel: %p\n", &numero);
    printf("Valor acessado pelo ponteiro: %d\n", *ponteiro);

    return 0;
}
